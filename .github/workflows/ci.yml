name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Add your language setup step here
      # Examples:
      #   - uses: actions/setup-node@v4
      #     with:
      #       node-version: '20'
      #   - uses: actions/setup-python@v5
      #     with:
      #       python-version: '3.12'

      - name: Install dependencies
        run: echo "Replace with your install command (e.g. npm install)"

      - name: Run tests
        run: echo "Replace with your test command (e.g. npm test)"
